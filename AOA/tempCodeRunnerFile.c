int k=0;
    // while(m > 0) {
    //     if(m >= knapsack[k].w) {
    //         sp += knapsack[k].p;
    //         m -= knapsack[k].w;
    //         printf("Select Item I%d.", knapsack[k].id);
    //         k++;
    //     }
    //     else {
    //         sp += knapsack[k].ratio * m;
    //         m = 0;
    //         printf("Select %f part of Item I%d.", knapsack[k].ratio, knapsack[k].id);
    //     }
    // }

    // printf("The total profit is %f.", sp);